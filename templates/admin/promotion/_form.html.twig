{{ form_start(form) }}
    {{ form_row(form.degree) }}
    {{ form_row(form.year) }}
    {{ form_row(form.startDate) }}
    {{ form_row(form.endDate) }}
    {{ form_row(form.notes) }}
<input type="submit" value="Valider">
{{ form_end(form) }}